# Railway Postgres — paste "DATABASE_URL" from Railway → Postgres → Connect / Variables
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/railway"

# Dev only: API server port (Vite proxies /api → this port)
API_PORT=3001

# Production: HTTP port (Railway sets PORT automatically — do not set locally unless needed)
# PORT=8080
