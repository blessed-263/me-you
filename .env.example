# --- Local dev ---
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/railway"
API_PORT=3001

# --- Vercel (frontend) — set in Vercel project → Environment Variables ---
# Railway API URL, no trailing slash (required for RSVP on production site)
# VITE_API_URL="https://your-service.up.railway.app"

# --- Railway (backend API) — same list as local if you test production frontends ---
# ALLOWED_ORIGINS="http://localhost:3000,http://127.0.0.1:3000,https://your-site.vercel.app,https://youandmeafrica.com,https://www.youandmeafrica.com"
# Or a single origin:
# FRONTEND_URL="https://your-site.vercel.app"
# SERVE_STATIC=false is set by npm run start:api (API-only; do not set on Vercel)

RESEND_API_KEY="re_xxxxxxxx"
RESEND_FROM_EMAIL="You & Me <rsvp@events.youandmeafrica.com>"
# Comma-separated for multiple admin inboxes
RSVP_NOTIFY_EMAIL="info@youandmeafrica.com, tumi@youandmeafrica.com"
# RSVP_TEST_EMAIL="you@example.com"
